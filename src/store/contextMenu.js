const contextMenuSlice = (set, get) => ({
  showContextMenu: false,
  upgradingSand: false,
  levelingUp: false,
});

const contextMenuActions = (set, get) => ({
  // stuff
});

export { contextMenuSlice, contextMenuActions };
